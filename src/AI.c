#include <stdio.h>
#include "AI.h"
#include "Eval.h"

/*
This file is to be worked on in the future
*/

void initAI(struct Player* AI, int dificulty) {
	AI->dificulty = dificulty;
}

/*
int getAIMove(int postFlop, int check, int AIBalance, int bet, Player AI, Player opponent) {
	if (AI.dificulty == 1) {
		if (check == 1) {
			if (postFlop && AI.handVal > 2 ) {
				return 50;
			}
			else if ()
			else {
				return 0;
			}
		}
		else {

		}
	}
	else if (AI.dificulty == 2) {

	}
	else if (AI.dificulty == 3) {

	}
}
*/